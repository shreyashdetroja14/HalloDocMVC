@model HalloDocServices.ViewModels.AdminViewModels.SendAgreementViewModel

<div class="modal fade" data-bs-backdrop="static" id="send-agreement-modal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-3">
            <div class="modal-header fw-bold bg-info text-white">
                <h1 class="modal-title fs-5 " id="modaltitle">Send Agreement</h1>

                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                
                <p class="fs-6 text-secondary">
                    To Send Agreement please make sure you are updating the correct contact information below for the responsible party.
                </p>

                @if (Model.IsAgreementSent == true)
                {
                    <div class="alert alert-warning" role="alert">
                        The agreement has already been sent for this request.
                    </div>
                }

                <form asp-action="SendAgreement" id="send-agreement-form" class="" >
                    <input type="number" asp-for="RequestId" hidden />

                    <div class="form-floating mb-3">
                        <input type="text" asp-for="Email" class="form-control" id="Email" placeholder="name@example.com" >
                        <label for="Email">Email address</label>
                        <span asp-validation-for="Email" class="text-danger"></span>
                        <div class="invalid-feedback">
                            Please enter an email.
                        </div>
                    </div>
                    <div class="form-floating mb-3">
                        <input type="number" asp-for="PhoneNumber" class="form-control" id="PhoneNumber" placeholder="PhoneNumber" >
                        <label for="PhoneNumber">Phone Number</label>
                        <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                        
                        <div class="invalid-feedback">
                            Please enter a phone number.
                        </div>
                    </div>

                    <div class=" d-flex justify-content-end">
                        <input type="submit" value="Send" class="btn btn-info text-white px-3 py-2" />
                        <button type="button" class="btn btn-outline-info px-3 py-2 ms-3" data-bs-dismiss="modal">Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>



